version: "3.9"

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: driving-school-db
    restart: unless-stopped
    environment:
      TZ: Europe/Warsaw
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: driving_school
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d driving_school || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - driving-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: driving-school-redis
    restart: unless-stopped
    environment:
      TZ: Europe/Warsaw
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - driving-network

  # Next.js Application (dev + hot reload)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: driving-school-app
    restart: unless-stopped
    environment:
      TZ: Europe/Warsaw
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres123@postgres:5432/driving_school?schema=public
      DIRECT_URL: postgresql://postgres:postgres123@postgres:5432/driving_school?schema=public
      REDIS_URL: redis://redis:6379
      NEXTAUTH_URL: http://localhost:3000
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NEXTAUTH_SECRET: development-secret-key-change-in-production
      # Якщо у рідкісних випадках не бачить зміни — розкоментуй дві нижні:
      # CHOKIDAR_USEPOLLING: "true"
      # WATCHPACK_POLLING: "true"
      NEXT_TELEMETRY_DISABLED: "1"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "3000:3000"
    # Монтуємо весь проект (зручно для hot-reload) + ізолюємо node_modules та .next у named volumes
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
      - next-cache:/app/.next
    networks:
      - driving-network
    # Авто-інсталяція залежностей, Prisma push + generate, сид, dev-сервер з 0.0.0.0
    command: >
      sh -c "
        # 1) Встановити залежності, якщо немає next
        if [ ! -x node_modules/.bin/next ]; then
          echo '📦 Installing dependencies...';
          if [ -f package-lock.json ]; then npm ci; else npm install; fi;
        fi &&

        # 2) Чекаємо БД
        echo '⏳ Waiting for database connection...' &&
        until nc -z postgres 5432; do echo 'Waiting for PostgreSQL...'; sleep 2; done &&

        # 3) Prisma
        echo '🔃 Prisma db push...' &&
        npx prisma db push --skip-generate --accept-data-loss &&
        echo '🧬 Prisma generate...' &&
        npx prisma generate &&

        # 4) Seed (не фатально у dev)
        if [ -f prisma/seed/index.js ]; then
          echo '🌱 Seeding database...'; node prisma/seed/index.js || echo '⚠️ Seed failed (non-fatal in dev)';
        fi &&

        # 5) Старт dev-сервера
        echo '🚀 Starting Next.js dev server...' &&
        npm run dev
      "

  # Adminer для управління БД (порт змінив на 8081, бо 8080 займає Portainer/Traefik)
  adminer:
    image: adminer:latest
    container_name: driving-school-adminer
    restart: unless-stopped
    environment:
      TZ: Europe/Warsaw
      ADMINER_DEFAULT_SERVER: postgres
      ADMINER_DESIGN: pepa-linha
    ports:
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - driving-network

networks:
  driving-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local
  node_modules:
    driver: local
  next-cache:
    driver: local
